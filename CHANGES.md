# 택시 시계열 분석 노트북 — 수정 내역 (CHANGES)

> 기준: 프로젝트 데이터 명세서(`DATA_DICTIONARY.md`) + 테이블정의서(`테이블정의서_20260520.xlsx`)
> 작업 원칙: ① 명세서를 정답으로 정합성 교정 ② 실행을 막는 버그 제거 ③ 외부데이터 활용·시계열 깊이 보강
> 폐쇄망 반입 환경이므로 **경로(`D012_PATH`, `EXT_DIR` 등)는 그대로 두었습니다.** 환경에 맞게 직접 조정하세요.

---

## 공통 사항

- 모든 노트북에서 택시 원본(`DC_TBYXD012.csv`)은 **청크 단위 누적 집계**로 처리합니다(6억 건 규모를 메모리에 올리지 않음).
- 컬럼명·파일명은 전부 명세서/테이블정의서 기준으로 통일했습니다. 핵심 확인 사항:
  - `RIDE_DTIME`/`ALIGHT_DTIME` = `NVARCHAR(14)` → `YYYYMMDDHHMMSS` 14자리. `format='%Y%m%d%H%M%S'` 맞음.
  - `PAY_AMT` = **결제금액(단건)**. 운임(`CARD_RIDE_FARE`+`CASH_RIDE_FARE`)과 다릅니다. 매출/요금 지표는 `PAY_AMT`를 프록시로 쓰되 라벨을 명확히 표기.
  - 시간별 ASOS에는 **눈(적설) 컬럼이 없습니다.** 눈이 필요한 분석은 일별 ASOS의 `snow_depth`를 `date`로 조인.

### 발견된 핵심 버그 패턴 (A그룹 공통)
청크 집계로 결과 변수를 만들어 놓고, **바로 다음 셀에서 존재하지 않는 통짜 `df`/`d012`를 다시 참조**하는 구조였습니다. 그대로 실행하면 `NameError`로 죽습니다. 해당 노트북들은 청크 집계 결과로 일원화하여 재작성했습니다.

---

## 01_weather_demand — 전면 재작성

**문제**
- 외부 날씨가 Open-Meteo 스키마(`weather/seoul_weather_2018_2026.csv`, `temperature/precipitation/snowfall/weather_code`)였음 → 명세서의 ASOS와 불일치. `snowfall` 등은 KeyError.
- 섹션2 첫 셀이 존재하지 않는 `d012` 재참조(죽는 버그).
- 청크별 평균을 다시 단순 평균으로 합쳐 가중치 오류.
- "강수 탄력성"을 비 오는 시간만 단순회귀로 산출 → 시간대 교란. `PAY_AMT`는 가격이 아니므로 가격탄력성도 아님.

**수정**
- 날씨를 **ASOS 시간별**(`weather_asos_hourly_seoul_2018_2026.csv`)로 교체, 컬럼 매핑(`temp→temperature`, `rainfall→precipitation`), `hour` 문자열→int, 강수 결측 0 처리.
- 눈은 **일별 ASOS `snow_depth`**를 `date`로 조인.
- 캘린더(`day_name/is_weekend/is_holiday`) 정식 조인.
- 청크 집계를 sum/count 분리 후 마지막에 1회 나눗셈 → 정확한 가중평균.
- 탄력성을 **"강수 수요반응"**으로 재정의: 같은 시간대 내 맑음 대비 변화율(시간대 고정효과 통제).
- 시계열 보강: 일별 7·30일 이동평균, **(월×요일) 매칭 비교**로 코로나·계절 시기효과를 흡수한 강수 순효과.

---

## 02_transit_mode_share — 전면 재작성

**문제**
- 지하철 데이터가 `transit/seoul_subway_hourly_ridership_all.csv` + `사용월/호선명/지하철역` + `'04시-05시 승차인원'` 형식 → 명세서의 `subway_ridership_2024.csv`(`수송일자/호선/역명` + `06-07시간대` 형식)와 불일치.
- 섹션2/4/6이 통짜 `d012` 재참조(죽는 버그).

**수정**
- 명세서 지하철 스키마로 교체. 시간대 컬럼(`06시이전`~`24시이후`)을 long format으로 변환, 대표 시(hour) 매핑.
- 지하철은 **2024년만 존재**하므로 택시도 2024년으로 한정해 공정 비교(분담률·월별 추이).
- 호선별 심야 비중은 `호선` 기준 집계로 수정.

---

## 03_commercial_vacancy — 재작성 (상가 데이터는 별도 보유 전제)

**문제**
- 섹션2/4/7이 통짜 `d012` 재참조(죽는 버그). 그리드 재배정 로직이 여러 셀에 흩어져 있었음.

**수정**
- 그리드별 하차건수·빈차거리, 행정동별 승차를 **청크 단계에서 집계**. 후속 분석은 이 결과(`taxi_grid`, `grid_hour`, `taxi_dong`)로 연결.
- 상가업소(`commercial/seoul_commercial.csv`)는 명세서에 없으나 별도 보유하신 데이터로 간주하고 그대로 사용.
- 좌표 변환(`POS_Y/1e7`)은 원본 로직 유지 — 실제 반입 파일 좌표 단위에 맞게 확인 필요(노트북 상단에 명시).

---

## 04_od_matrix — 전면 재작성

**문제**
- 청크 집계로 `od_matrix`를 만든 뒤, 이어지는 7개 셀 전부가 통짜 `df` 재참조(죽는 버그). 실행 즉시 중단.

**수정**
- OD쌍 집계 + 시간대/요일 패턴 + 유입·유출 + 거리 분포를 모두 **청크 누적**으로 일원화.
- 히트맵/Top20/불균형/거리분포/요약을 청크 결과 기반으로 재구성.

---

## 05_driver_profitability — 전면 재작성

**문제**
- 청크에서 `df_driver`를 만든 뒤, 섹션1부터 통짜 `df` 재참조(죽는 버그).

**수정**
- 운전자별(매출·건수·거리·빈차·운행시간·수수료)과 시간대×요일 매출을 **청크 누적**으로 집계.
- 실차율/시간당매출/순매출을 sum 기반으로 정확히 산출. 등급(A~D)·수수료 분석을 청크 결과로 연결.
- `PAY_AMT`는 결제금액 프록시임을 상단에 명시.

---

## 06_covid_impact — 전면 재작성

**문제**
- 청크에서 `monthly` 등을 만든 뒤, 섹션1부터 통짜 `df` 재참조(죽는 버그).
- 외부 데이터(확진자·거리두기)를 조인하지 않고 기간 라벨만 사용.

**수정**
- 월별/시간대/요일/행정동 집계를 **청크 누적**으로 일원화.
- **외부 조인 추가**: `covid_korea_2018_2026.csv`(월 확진자 합), `social_distancing_daily.csv`(월 최고 거리두기 단계)를 월별 시계열에 오버레이.
- 행정동 Pre→Post 변화율, 평균거리/요금 추이, 심야 비율 추이를 청크 결과로 재구성.

---

## 07_vacancy_simulation — 전면 재작성

**문제**
- 청크에서 빈차 샘플/집계를 만든 뒤, 섹션1부터 통짜 `df` 재참조(죽는 버그).
- 청크 경계를 넘는 같은 차량의 연속 운행이 끊겨 빈차시간 누락.

**수정**
- **차량 carry-over**: 각 청크의 차량별 마지막 운행을 다음 청크로 이월해 경계 빈차시간을 정확히 계산.
- 빈차 분포/시간대·행정동 효율/재배치 시뮬레이션을 청크 결과(`vacancy` 샘플, `dong_vac`, `hr_vac`)로 연결.

---

## 08_holiday_event — 재작성 (외부 캘린더 활용)

**문제**
- 청크에서 `daily`를 만든 뒤, 통짜 `df` 재참조(죽는 버그).
- 공휴일을 노트북 안에 **하드코딩**(유지보수·정합성 취약).

**수정**
- 공휴일을 외부 `calendar_2018_2026.csv` / `holidays_2018_2026.csv`로 대체. 설날·추석 당일은 `holiday_name`에서 추출.
- 일별·시간대별 집계를 청크 누적으로 일원화. 명절 전후/연말연시/심야(금·토·공휴일 전날) 분석을 캘린더 기반으로 재구성.

---

## 09_dead_time — 통과 + 시계열 보강

**상태**: 버그 없음(numpy 청크 누적이 잘 되어 있음), 명세서 일치.
**보강**: 일별 수요 7·30일 이동평균 추세, **월별 평균 빈차율 추세**(죽은 시간이 시기적으로 악화/개선되는지) 추가.

## 10_asymmetric_od — 통과 + 시계열 보강

**상태**: 버그 없음(OD 비대칭·반전·빈차낭비 모두 청크 누적).
**보강**: 일별 수요 추세 섹션 추가.
**참고**: 도심/강남 행정동 코드(`11010` 등)는 데모용 가정값 — 실제 행정동 코드 체계에 맞게 조정 권장.

## 11_fare_psychology — 통과 + 시계열 보강

**상태**: 버그 없음(요금 히스토그램·거리·빈차시간 모두 청크/샘플 누적). 외부데이터 미사용(D012 단독).
**보강**: 일별 수요 추세 + **만원 직전(9500~9990원) 하차 비율의 월별 추세**(요금 심리가 시간에 따라 강화/약화되는지).

## 13_golden_route — 통과 + 시계열 보강

**상태**: 버그 없음(상·하위 운전자 OD/시간대/지역/실차율 모두 청크 누적).
**보강**: 일별 수요 추세 섹션 추가.

## 14_multi_period_decomposition — 통과 (보강 불요)

**상태**: 버그 없음. 외부데이터(ASOS daily, calendar) 명세서 일치. FFT+Wavelet+STL로 **이미 시계열 분석의 핵심**. 중복 보강은 넣지 않음.

## 15_causal_impact — 통과 (보강 불요)

**상태**: 버그 없음. 외부 5종(weather/covid/distancing/events/calendar) 정확히 조인. DID·반사실 추정으로 이미 시계열 인과 분석 충실. 그대로 둠.

## 16_change_point_detection — 통과 + 보조 보강

**상태**: 버그 없음. PELT 변화점 + 이벤트 자동 매칭, 외부데이터 일치.
**보강**: 일별 수요 7·30일 이동평균 추세(변화점 해석 시 참고용) 추가.

## 17_weather_elasticity — 통과 + 시계열 보강

**상태**: 버그 없음. ASOS hourly/daily + calendar 정확히 조인. arc elasticity + 지역 민감도까지 우수.
**보강**: 일별 수요 추세 섹션 추가(`TAXI_FILE` 변수 사용).

## 18_subway_substitution — 통과 + 시계열 보강

**상태**: 버그 없음. `subway_ridership_2024.csv` 명세서 스키마 정확히 사용. Granger 인과 + 막차 급등 분석 우수.
**보강**: 일별 수요 추세 섹션 추가.

## 19_explainable_anomaly — 통과 (보강 불요)

**상태**: 버그 없음. 외부 전종 조인. IQR/Z-score/IsolationForest 합의 이상치 + LightGBM/SHAP 설명까지 가장 완성도 높음. 그대로 둠.

---

## 남은 확인 권장 사항

1. **좌표 단위**(03, 07): `POS_X`(도) / `POS_Y/1e7` 변환이 실제 반입 파일과 맞는지 1개 청크로 검증 후 진행.
2. **행정동 코드 체계**(10): 도심/강남 매핑값이 실제 코드와 일치하는지.
3. **운임 분해**: 기사 실수령/순매출을 더 정밀히 보려면 `CARD_RIDE_FARE`+`CASH_RIDE_FARE`, `PLTF_FEE_AMT`, `DIS_AMT`로 분해(05, 11 확장 여지).
4. 각 노트북은 **실데이터 미실행 상태**입니다(폐쇄망). 문법·정합성·로직은 검증했으나, 실제 값/분포는 반입 후 첫 실행에서 확인하세요.

# 신규 노트북 버그 수정 내역 (12, 22, 23)

> 대상: `12_rain_blackhole`, `22_platform_efficiency`, `23_fare_hike_elasticity`
> 원칙: 실행을 막는 버그만 외과적으로 수정. 나머지 로직·구조는 그대로 유지.
> 검증: 3개 모두 nbformat 유효 + 코드 셀 문법 오류 0개 확인 (실데이터 미실행이므로 값 검증은 반입 후 첫 실행에서 필요).

---

## 12_rain_blackhole — 시계열 보강 셀 (실행 시 즉사 → 수정)

**문제 (NameError로 바로 죽음)**
추가된 "비 vs 맑은 날 일별 추세" 셀이 두 가지로 깨졌습니다.
1. **삭제된 변수 참조**: 맨 위 데이터 로드 셀이 끝에서 `del weather, wl, wd`로 `weather`를 이미 삭제합니다. 그런데 이 보강 셀이 `weather.loc[...]`를 다시 참조 → `NameError`.
2. **없는 컬럼 참조**: `weather['rain_flag']`, `weather['date_str']`를 찾는데, 실제로 만든 컬럼은 `is_rain`, `date`였습니다. 두 이름은 존재한 적이 없습니다.

**수정**
- `weather` 대신, 노트북 끝까지 살아있는 `rain_set`(`'YYYYMMDD_HH'` 시간 단위 집합)을 재사용.
- `rain_set`은 시간 단위라 일별 추세용으로 **앞 8자리를 잘라 '비/눈 온 날' 날짜 집합**(`_rain_days`)으로 변환. "그 날 비가 한 시간이라도 왔으면 비 온 날"로 정의.
- 일별 수요를 청크로 다시 집계해 비/맑음으로 분리, 각각 7·30일 이동평균.
- **해석 주의 주석 추가**: 비는 장마철에 몰려 계절 수요와 섞이므로, 이 그래프를 "비가 수요를 늘린다"의 근거로 쓰지 말 것(그 분석은 본문 블랙홀 섹션이 시간대 매칭으로 이미 수행). 이 셀은 비/맑음 각각의 **장기 추세 보조 시각화**로만 사용.

---

## 22_platform_efficiency — 플랫폼 전후 비교 셀 (무의미한 빈 비교 → 정정)

**문제 (즉사하진 않지만 분석이 헛돎)**
"시간대별 빈차율: 2018 이전 vs 이후" 셀에서 `hour_year[hour_year['year'] < 2018]`로 '플랫폼 이전(~2017)' 그룹을 만듭니다. 그런데 **D012 데이터는 2018-01부터 시작**이라 이 그룹은 항상 비어 있습니다. `if len(pre_platform) > 0` 가드 덕분에 죽지는 않지만, "플랫폼 전후 비교"라는 제목과 달리 실제로는 비교가 성립하지 않습니다. 카카오T 도입(2018) "이전" baseline이 데이터에 없다는 게 이 분석의 근본 한계입니다.

**수정**
- '플랫폼 이전(~2017)' 비교를 제거. 데이터 내에서 의미 있는 **시기 비교**로 교체: 코로나 전(2018–2019) / 코로나(2020–2022) / 코로나 후(2023~).
- 제목을 "플랫폼 전후 비교" → "데이터 내 시기 비교"로 변경.
- **출력에 한계 명시**: "데이터가 2018부터라 카카오T 도입 이전 baseline 관측 불가 → 이 비교는 플랫폼 인과효과가 아니라 시기별 기술 통계"라고 print.

**남은 해석 주의 (코드는 그대로, 결론만 톤다운 권장)**
실차율·Gini·CV의 연도별 추이 상승/변화를 "플랫폼 효과"로 단정하지 말 것. 2020–2022 코로나가 끼어 있어 플랫폼 효과와 분리되지 않습니다. 이 노트북은 "플랫폼 인과 분석"이 아니라 "2018–2024 운영 효율성·매출 불평등의 기술적(descriptive) 시계열"로 포지셔닝하는 것이 정직합니다.

---

## 23_fare_hike_elasticity — hike 판정 인덱싱 (위태로운 코드 → 안전하게) + DID 명칭 보강

**문제 1 (인덱싱 깨질 위험)**
청크 집계 셀에서:
```python
sub = chunk[in_h1 | in_h2].copy()
sub['hike'] = np.where(in_h1[in_h1 | in_h2], 1, 2)   # ← 위태로움
```
`sub`는 이미 필터된 프레임인데 `in_h1`(원본 chunk 길이의 불리언)을 `(in_h1|in_h2)`로 인덱싱해 길이를 맞추려 한 코드입니다. 의도는 맞지만 정렬·길이가 어긋나면 조용히 잘못된 `hike` 라벨이 붙을 수 있습니다.

**수정**
- 1차(2018)·2차(2023) 분석 윈도우는 서로 겹치지 않으므로, `sub`의 날짜로 직접 판정하도록 단순화:
```python
sub['hike'] = np.where(sub['date'] <= h1_end, 1, 2)
```
- 길이/정렬 문제가 원천 제거되고 가독성도 좋아짐.

**문제 2 (방법론 명칭 — 코드는 정상, 마크다운 보강)**
"DID(Difference-in-Differences)"라고 했으나 두 인상이 **모두 처치(요금 인상)**이고 별도 통제군이 없어 엄밀한 DID가 아닙니다.

**수정**
- DID 설명 마크다운에 주의 문구 추가: 엄밀한 DID가 아니라 **단절적 시계열(ITS) + 두 사건 비교**에 가까우며, `post:hike2`는 "2차가 1차 대비 준 추가 효과"로 해석하고 통제군 대비 인과효과로 읽지 말 것. 요일·날씨·공휴일을 통제한 **`post` 계수만** 수요 변화 추정치로 보고.

**남은 해석 주의 (수정 안 함, 사용 시 주의)**
7번 요약의 "원시 탄력성"(`raw_change/pct_hike`)은 요일·계절·코로나 미통제 단순 전후 차이입니다. 특히 2차 인상(2023-02)은 코로나 회복 국면과 겹쳐 착시가 생길 수 있으니, 보고는 통제 후 회귀계수 기준으로 하고 raw는 참고용으로만.

---

## 종합

| 노트북 | 버그 심각도 | 수정 결과 |
|--------|------------|-----------|
| 12 | 즉사 (NameError) | 살아있는 `rain_set` 재사용으로 정상화 |
| 22 | 헛돎 (빈 비교) | 시기 비교로 정정 + 한계 명시 |
| 23 | 잠재적 오라벨 | 날짜 직접 판정으로 안전화 + 명칭 보강 |

세 노트북 모두 문법·구조 검증 통과. 실데이터 반입 후 첫 실행에서 값·분포만 확인하면 됩니다.

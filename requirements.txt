# 택시 시계열 분석 노트북 의존성
# 폐쇄망 반입 시: pip download -r requirements.txt -d ./wheels
# 설치: pip install --no-index --find-links=./wheels -r requirements.txt

# 데이터 처리
numpy>=1.24
pandas>=2.0
scipy>=1.10

# 시각화
matplotlib>=3.7
seaborn>=0.12
plotly>=5.15

# 시계열 분석
statsmodels>=0.14        # STL 분해, Granger 인과, 회귀
ruptures>=1.1.9          # 변화점 탐지 (PELT)

# ML / 설명
scikit-learn>=1.3        # Isolation Forest, Ridge 등
lightgbm>=4.0            # Explainable 이상 탐지
shap>=0.42               # SHAP 워터폴 차트

# 시스템
psutil>=5.9              # 메모리 모니터링
